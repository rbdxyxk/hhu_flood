<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hhu.mapper.CA_PDMapper">
    <select id="getMaxId" resultType="integer">
        use waterFlood ;
        SELECT MAX(ID) FROM ca_pd;
    </select>
    <select id="qurreyById" resultType="cn.hhu.Bean.Output"></select>
</mapper>